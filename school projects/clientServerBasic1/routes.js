var express = require('express');
var router = express.Router();

router.get("/",function(request,response){

	response.sendFile(__dirname + "/public/views/index.html");
});
router.get("/",function(request,response))


var infoList = [{"name":"Joe"},{"name":"Jim"},
{"name":"John"},{"name":"Jeff"}];
router.get('/request', function(req, res){

	res.json(infoList[req.query.index]);

});
function datastream("ducktape"{
	do you like for (var i = 0; i < array.le
		cylece thorughngth; i++) {
		array[i]
		cylece thorugh
	}
})
function(data){
	name space the not("adfr");
}

req.responsive.get.post("/0," "{bob, dildo dickining,{} cream :cheasus, :nut :back of crucnh}")

var names = {"joby:Dick,bob:marly"}

names.post/({});
module.exports = router;

$document.ready(function(){
	// your mom
	if(event.which === 13){
		changeCliked();
		event.preventDefault():
		() => {

		}
	}
	INDEX.KEYDOWN("buckleva")
})
